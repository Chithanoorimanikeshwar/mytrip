import { SeatsAvailableDirective } from './seats-available.directive';

describe('SeatsAvailableDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatsAvailableDirective();
    expect(directive).toBeTruthy();
  });
});
